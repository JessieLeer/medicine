import Vue from 'vue'
import axios from 'axios'

// initial state
const state = {
	serverUrl: 'http://192.168.1.63:8080'
}

// getters
const getters = {
}

// actions
const actions = {
	
}

// mutations
const mutations = {
	
}

export default {
  state,
  getters,
  actions,
  mutations
}